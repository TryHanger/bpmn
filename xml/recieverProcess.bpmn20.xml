<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             xmlns:bpmndi="http://www.omg.org/spec/BPMN/20100524/DI"
             xmlns:dc="http://www.omg.org/spec/DD/20100524/DC"
             xmlns:di="http://www.omg.org/spec/DD/20100524/DI"
             targetNamespace="http://example.com">

  <!-- Тот же самый сигнал -->
  <signal id="Signal_TriggerReceiver" name="Trigger Receiver Process" />

  <process id="receiverProcess" name="Receiver Process" isExecutable="true">

    <!-- Signal Start Event -->
    <startEvent id="startBySignal" name="Запущен по сигналу">
      <signalEventDefinition id="signalStartDef" signalRef="Signal_TriggerReceiver" />
    </startEvent>

    <userTask id="managerTask" 
              name="Задача Менеджера" 
              flowable:assignee="manager" />

    <sequenceFlow id="flow1" sourceRef="startBySignal" targetRef="managerTask" />
    <sequenceFlow id="flow2" sourceRef="managerTask" targetRef="endEvent" />

    <endEvent id="endEvent" name="End" />

  </process>

  <bpmndi:BPMNDiagram id="BPMNDiagram_receiver">
    <bpmndi:BPMNPlane id="BPMNPlane_receiver" bpmnElement="receiverProcess">
      <bpmndi:BPMNShape id="start_di" bpmnElement="startBySignal">
        <dc:Bounds x="150" y="100" width="36" height="36" />
      </bpmndi:BPMNShape>
      <bpmndi:BPMNShape id="task_di" bpmnElement="managerTask">
        <dc:Bounds x="250" y="80" width="100" height="80" />
      </bpmndi:BPMNShape>
      <bpmndi:BPMNShape id="end_di" bpmnElement="endEvent">
        <dc:Bounds x="400" y="100" width="36" height="36" />
      </bpmndi:BPMNShape>

      <bpmndi:BPMNEdge id="flow1_di" bpmnElement="flow1">
        <di:waypoint x="186" y="118"/>
        <di:waypoint x="250" y="118"/>
      </bpmndi:BPMNEdge>
      <bpmndi:BPMNEdge id="flow2_di" bpmnElement="flow2">
        <di:waypoint x="350" y="118"/>
        <di:waypoint x="400" y="118"/>
      </bpmndi:BPMNEdge>
    </bpmndi:BPMNPlane>
  </bpmndi:BPMNDiagram>

</definitions>